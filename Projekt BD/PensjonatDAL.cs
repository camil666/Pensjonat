using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Windows.Forms;
using System.Data.Objects;
using System.Data;

namespace Projekt_BD
{
    internal class PensjonatDAL
    {
        /*
        #region Recepcjonista
        public static void AddNewClient(Guest newGuest)
        {
            using (PensjonatEntities context = new PensjonatEntities())
            {
                try
                {
                    context.Guest.AddObject(newGuest);
                    context.SaveChanges();
                }
                catch (Exception ex)
                {
                    MessageBox.Show(ex.InnerException.Message);
                }
            }
        }

        #endregion

        #region Klient

        #endregion

        #region Pracownik
        #endregion
    
         */
    }
}
