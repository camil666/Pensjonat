namespace Projekt_BD.Enums
{
    /// <summary>
    /// Enum for roles.
    /// </summary>
    public enum Roles
    {
        Administrator = 0,

        Receptionist = 1,

        Manager = 2,

        Employee = 3,

        Root = 4
    }
}
